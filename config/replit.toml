# Replit overlay — paper mode on port 8080
[mode]
run_mode = "paper"
dry_run  = true

[risk]
equity_usd = 5000.0
risk_per_trade_pct = 0.5
max_open_positions = 3
max_daily_loss_pct = 3.0
max_drawdown_pct   = 10.0

[monitoring]
metrics_bind = "0.0.0.0:8080"
